# Full URL to the backend API (no trailing slash).
# Leave empty to use the local dev proxy (vite.config.ts).
# Required in production / standalone deployments.
VITE_API_URL=http://localhost:8080/api
